{
    "id": "cuda-uv",
    "version": "1.0.4",
    "name": "CUDA 12 + UV Template",
    "description": "Professional CUDA 12 development environment with 'uv' for Python package management and shared cache.",
    "options": {
        "pythonVersion": {
            "type": "string",
            "description": "Python version to use (e.g., 3.12, 3.11)",
            "default": "3.12"
        },
        "projectName": {
            "type": "string",
            "description": "Project name for 'uv init'. If empty, the current directory name will be used. (Ignored if 'Initialize uv project' is disabled)",
            "default": ""
        },
        "runInitialSetup": {
            "type": "boolean",
            "description": "Initialize a full 'uv' project (uv init)? If disabled, only a virtual environment will be created.",
            "default": true
        }
    }
}
